---
import { projects } from "../data/project.json";
import ProjectsList from "../components/ProjectsList.astro";
---

<section class="projects" id="projects" data-aos="fade-up" data-aos-duration="1000" data-aos-delay="300">
    <div class="container">
        <h2>Projects</h2>
        <hr />
        <div class="row justify-content-end">
            {projects.map((project) => <ProjectsList {...project} />)}
        </div>
    </div>
</section>
